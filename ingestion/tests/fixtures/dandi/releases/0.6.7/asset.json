{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Asset",
  "description": "A DANDI asset (file).",
  "type": "object",
  "properties": {
    "identifier": {
      "title": "Identifier",
      "description": "The identifier for this asset.",
      "type": "string"
    },
    "path": {
      "title": "Path",
      "description": "The path of this asset within the dandiset.",
      "type": "string"
    },
    "contentSize": {
      "title": "Content Size",
      "description": "The size of this asset in bytes.",
      "type": "integer",
      "minimum": 0
    },
    "encodingFormat": {
      "title": "Encoding Format",
      "description": "The media type of this asset.",
      "type": "string"
    },
    "dateModified": {
      "title": "Date Modified",
      "description": "The date this asset was last modified.",
      "type": "string"
    },
    "schemaKey": {
      "title": "Schema Key",
      "type": "string",
      "const": "Asset"
    }
  },
  "required": ["identifier", "path", "schemaKey"],
  "$defs": {
    "DigestType": {
      "title": "DigestType",
      "type": "string",
      "enum": ["md5", "sha256", "dandi:dandi-etag"]
    }
  }
}
