semantic:
  data_type: string
  constraints:
    allowed_values:
    - mag
    - phase
    - real
    - imag
  response_options:
  - value: mag
    label: mag
    ontology_term: https://schema.undata.live/values/mag_86dw07
  - value: phase
    label: phase
    ontology_term: https://schema.undata.live/values/phase_70ryu1
  - value: real
    label: real
    ontology_term: https://schema.undata.live/values/real_1kt06h
  - value: imag
    label: imag
    ontology_term: https://schema.undata.live/values/imag_f6f2nv
  value_domain: categorical
provenance:
- source: bids
  class: entities
  name: part
  description: 'This entity is used to indicate which component of the complex

    representation of the MRI signal is represented in voxel data.

    The `part-<label>` entity is associated with the DICOM Tag

    `0008, 9208`.

    Allowed label values for this entity are `phase`, `mag`, `real` and `imag`,

    which are typically used in `part-mag`/`part-phase` or

    `part-real`/`part-imag` pairs of files.


    Phase images MAY be in radians or in arbitrary units.

    The sidecar JSON file MUST include the `"Units"` of the `phase` image.

    The possible options are `"rad"` or `"arbitrary"`.


    When there is only a magnitude image of a given type, the `part` entity MAY be

    omitted.

    '
  generated_at: '2026-03-20T22:15:23.043717+00:00'
  attributed_to: urn:undata:ingestion-pipeline
  activity: ingestion
