semantic:
  data_type: string
provenance:
- source: bids
  class: entities
  name: volume
  description: 'The `voi-<label>` entity can be used to distinguish acquisitions localized
    to different regions.

    The label SHOULD be the name of the body region or part scanned.

    If used, the fields `"BodyPart"` and `"BodyPartDetails"` MUST be defined in the
    JSON file.

    `BodyPartDetailsOntology` is OPTIONAL to also include.

    '
